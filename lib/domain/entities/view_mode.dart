/// 파일 목록을 어떤 형태로 보일지 고르는 보기 모드.
///
/// 셋 다 같은 필터·선택·프리뷰 계약을 공유하고 표현만 다르다: [list]는 세로 목록,
/// [icon]은 그리드, [detail]은 태그를 컬럼으로 펼친 테이블이다. 워크스페이스에
/// 종속돼 `.filetagger/` 보기 설정에 함께 저장된다.
enum ViewMode { list, icon, detail }
